h1(id="header-title") freeCodeCamp Voting App
h2(id="poll-list-label") Polls
hr(class="title-hr")
ul(class="poll-list" id="poll-list")
    each poll in polls
        a(ui-sref='poll({id: "' + poll._id + '"})')
            li= poll.question